# Database Configuration
# For SQLite: sqlite:///path/to/database.db (relative to project root)
# For PostgreSQL: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///main_db.db

# Flask Configuration
# Set to 1 for development (uses Flask debug server)
# Set to 0 for production (uses waitress server)
FLASK_DEBUG=1

# Secret Key
# Generate a secure random key for production use
# Example: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production

# Migration Command (optional)
# Used for database migrations: init, migrate, or upgrade
# MIGRATE_CMD=
